return {
    "olimorris/onedarkpro.nvim",
    lazy = true,
    opts = {
        options = {
            transparency = true
        }
    },
    -- priority = 1000,
    -- config = function()
    --     vim.cmd("colorscheme onedarkpro")
    -- end,
}
